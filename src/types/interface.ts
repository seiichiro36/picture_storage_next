export interface Artwork {
    id: string;
    title: string;
    description?: string;
    imageUrl: string;
    tags?: string[];
    videoUrl?: string;
    createdAt: string;
    updatedAt: string;
  }
